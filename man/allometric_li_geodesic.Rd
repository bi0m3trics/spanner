% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allometric_algorithms.R
\name{allometric_li_geodesic}
\alias{allometric_li_geodesic}
\title{Allometric Li-Style Geodesic Segmentation}
\usage{
allometric_li_geodesic(
  seeds = NULL,
  hmin = 2,
  k = 12,
  max_jump = 1.5,
  bin_height = 1,
  crown_a = 0.35,
  crown_b = 0.65,
  max_crown_radius = NULL,
  center_drift = 1.5,
  density_radius = 1,
  gap_penalty = 3,
  geodesic_threshold = 5,
  use_connected_components = TRUE,
  min_component_size = 3,
  omp = NULL,
  crown_profile = c("beta", "parabolic", "cone"),
  verbose = FALSE
)
}
\arguments{
\item{seeds}{Optional seed treetops as \code{sf} points or \code{data.frame} with \code{X},
\code{Y}, and optional \code{Z}.}

\item{hmin}{Minimum point height to consider for assignment.}

\item{k}{Number of graph neighbors per point.}

\item{max_jump}{Maximum local jump distance used in graph construction.}

\item{bin_height}{Vertical bin size guiding top-down growth.}

\item{crown_a}{Allometric coefficient in crown radius envelope.}

\item{crown_b}{Allometric exponent in crown radius envelope.}

\item{max_crown_radius}{Optional hard cap for crown radius (\code{NULL} for none).}

\item{center_drift}{Maximum per-step drift for moving crown center.}

\item{density_radius}{Radius used to estimate local density gaps.}

\item{gap_penalty}{Weight for low-density/gap transitions.}

\item{geodesic_threshold}{Maximum accumulated geodesic cost for assignment.}

\item{use_connected_components}{If \code{TRUE}, penalize transitions across sparse
connected components.}

\item{min_component_size}{Minimum in-crown connected component size to keep.}

\item{omp}{Logical or \code{NULL}. If \code{NULL} (default), OpenMP is enabled on
Windows/Linux and disabled on macOS. If \code{TRUE}/\code{FALSE}, this overrides the
OS default.}

\item{crown_profile}{Named allometric profile (\code{"beta"}, \code{"parabolic"},
\code{"cone"}) or a custom function.}

\item{verbose}{If \code{TRUE}, emit high-level progress messages in R.}
}
\value{
A \code{PointCloudBased} \code{IndividualTreeSegmentation} algorithm object.
}
\description{
Builds a point-cloud-based individual tree segmentation algorithm object
compatible with \code{\link[lidR:segment_trees]{lidR::segment_trees()}}. The method is inspired by Li et al.
(2012), with allometric crown constraints, moving crown centers, and
geodesic growing over a local kNN graph.
}
\examples{
LASfile <- system.file("extdata", "ALS_Clip.laz", package = "spanner")
las <- lidR::readLAS(LASfile, select = "xyz")
las <- lidR::decimate_points(las, lidR::random_per_voxel(0.33, 1L))
las <- lidR::classify_ground(las, lidR::ptd())
las <- lidR::normalize_height(las, lidR::tin())
las_geodesic <- lidR::segment_trees(
  las,
  allometric_li_geodesic(
    seeds = NULL,
    hmin = 2,
    k = 12,
    max_jump = 1.5,
    bin_height = 1,
    crown_a = 0.35,
    crown_b = 0.65,
    max_crown_radius = NULL,
    center_drift = 1.5,
    density_radius = 1,
    gap_penalty = 3,
    geodesic_threshold = 5,
    use_connected_components = TRUE,
    min_component_size = 10,
    crown_profile = c("beta", "parabolic", "cone"),
    verbose = FALSE
  )
)
lidR::plot(las_geodesic, color = "treeID")
}
