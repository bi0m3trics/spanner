% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allometric_algorithms.R
\name{allometric_supervoxel_segment}
\alias{allometric_supervoxel_segment}
\title{Allometric Supervoxel Segmentation}
\usage{
allometric_supervoxel_segment(
  seeds = NULL,
  hmin = 2,
  voxel_size = 1.2,
  k_voxel = 18,
  min_voxel_points = 3,
  crown_a = 0.35,
  crown_b = 0.65,
  component_gap = 1.5,
  merge_threshold = 6,
  anisotropy_weight = 0.5,
  verticality_weight = 0.75,
  density_weight = 1,
  allometry_weight = 1.9,
  crown_profile = c("beta", "parabolic", "cone"),
  verbose = FALSE
)
}
\arguments{
\item{seeds}{Optional seed treetops as \code{sf} points or \code{data.frame} with \code{X},
\code{Y}, and optional \code{Z}.}

\item{hmin}{Minimum point height to consider for assignment.}

\item{voxel_size}{Voxel edge size in map units.}

\item{k_voxel}{Number of voxel neighbors in graph construction.}

\item{min_voxel_points}{Minimum points per voxel to keep as active.}

\item{crown_a}{Allometric coefficient in crown radius envelope.}

\item{crown_b}{Allometric exponent in crown radius envelope.}

\item{component_gap}{Maximum centroid-gap for component continuity.}

\item{merge_threshold}{Maximum local voxel-edge coherence cost.}

\item{anisotropy_weight}{Weight for anisotropy differences.}

\item{verticality_weight}{Weight for verticality differences.}

\item{density_weight}{Weight for voxel density-gap penalties.}

\item{allometry_weight}{Weight for allometric penalties.}

\item{crown_profile}{Named allometric profile (\code{"beta"}, \code{"parabolic"},
\code{"cone"}) or a custom function.}

\item{verbose}{If \code{TRUE}, emit high-level progress messages in R.}
}
\value{
A \code{PointCloudBased} \code{IndividualTreeSegmentation} algorithm object.
}
\description{
Builds a supervoxel-graph segmentation algorithm for
\code{\link[lidR:segment_trees]{lidR::segment_trees()}}. Occupied voxels are segmented first and resulting
IDs are propagated back to points. The implementation includes small-island
cleanup and backfilling so isolated unlabeled speckles are less likely to be
left behind inside crowns.
}
\examples{
LASfile <- system.file("extdata", "ALS_Clip.laz", package = "spanner")
las <- lidR::readLAS(LASfile, select = "xyz")
las <- lidR::decimate_points(las, lidR::random_per_voxel(0.33, 1L))
las <- lidR::classify_ground(las, lidR::ptd())
las <- lidR::normalize_height(las, lidR::tin())
las_sv <- lidR::segment_trees(
  las,
  allometric_supervoxel_segment(
    seeds = NULL,
    hmin = 2,
    voxel_size = 1.2,
    k_voxel = 18,
    min_voxel_points = 3,
    crown_a = 0.35,
    crown_b = 0.65,
    component_gap = 1.5,
    merge_threshold = 6,
    anisotropy_weight = 0.5,
    verticality_weight = 0.75,
    density_weight = 1,
    allometry_weight = 1.9,
    crown_profile = c("beta", "parabolic", "cone"),
    verbose = FALSE
  )
)
lidR::plot(las_sv, color = "treeID")
}
