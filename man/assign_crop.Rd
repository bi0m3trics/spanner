% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tree_Points_Methods.R
\name{assign_crop}
\alias{assign_crop}
\title{Tree-points method: fixed-radius crop}
\usage{
assign_crop(l = 1, circle = TRUE)
}
\arguments{
\item{l}{numeric.  Circle radius (metres) when \code{circle = TRUE}, or
half side-length when \code{circle = FALSE}.  Default \code{1.0}.}

\item{circle}{logical.  \code{TRUE} = circular crop (default); \code{FALSE} = square
crop.}
}
\value{
A closure of class \code{"spanner_tpt_mtd"} / \code{"function"} with
attribute \code{method_name = "crop"}.
}
\description{
Assigns points within a fixed circle (or square) around each tree centre
to that tree.  Points outside every crop receive \code{treeID = 0}.  Where
crops overlap, priority is given to the tree that appears first when
iterating in \code{map} row order.
}
\details{
Implemented via \code{C_tree_ids_crop()}.  Useful for conservative stem-only
point extraction where crown points should be excluded.
}
\examples{
\donttest{
map   <- find_trees(las, use_raster_eigen())
las_c <- tree_points(las, map, method = assign_crop(l = 1.5))
}

}
\seealso{
\code{\link[=tree_points]{tree_points()}}, \code{\link[=assign_voronoi]{assign_voronoi()}}, \code{\link[=assign_graph]{assign_graph()}}
}
