% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tree_Points_Methods.R
\name{assign_graph}
\alias{assign_graph}
\title{Tree-points method: graph propagation}
\usage{
assign_graph(...)
}
\arguments{
\item{...}{Named arguments forwarded to \code{\link[=segment_graph]{segment_graph()}}.}
}
\value{
A closure of class \code{"spanner_tpt_mtd"} / \code{"function"} with
attribute \code{method_name = "graph"}.
}
\description{
Wraps \code{\link[=segment_graph]{segment_graph()}} as a composable method object for \code{\link[=tree_points]{tree_points()}}.
Builds a k-nearest-neighbour graph over the point cloud and propagates
tree IDs from seed locations via shortest paths — the most spatially
accurate but slowest assignment method.
}
\details{
All arguments are forwarded verbatim to \code{\link[=segment_graph]{segment_graph()}}.  Refer to that
function's documentation for the full parameter list including \code{k},
\code{distance.threshold}, \code{use.metabolic.scale}, \code{subsample.graph},
\code{return.dense}, etc.
}
\examples{
\donttest{
map   <- find_trees(las, use_raster_eigen())
las_g <- tree_points(las, map,
           method = assign_graph(k = 50,
                                 distance.threshold = 0.5,
                                 subsample.graph = 0.1,
                                 return.dense = TRUE))
}

}
\seealso{
\code{\link[=tree_points]{tree_points()}}, \code{\link[=assign_voronoi]{assign_voronoi()}}, \code{\link[=assign_crop]{assign_crop()}},
\code{\link[=segment_graph]{segment_graph()}}
}
