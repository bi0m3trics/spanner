% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tree_Points_Methods.R
\name{assign_voronoi}
\alias{assign_voronoi}
\title{Tree-points method: Voronoi nearest-tree partition}
\usage{
assign_voronoi(max_dist = -1)
}
\arguments{
\item{max_dist}{numeric.  Points farther than this distance (m) from every
tree centre are left with \code{treeID = 0}.  Set to \code{-1} (default) to assign
\strong{all} points — a pure Voronoi partition.}
}
\value{
A closure of class \code{"spanner_tpt_mtd"} / \code{"function"} with
attribute \code{method_name = "voronoi"}.
}
\description{
Assigns every point to the closest tree centre in 2-D Euclidean space.
This is a complete spatial partition — every point receives a tree ID
unless \code{max_dist} is set.
}
\details{
Implemented via \code{C_tree_ids_voronoi()}.  For typical TLS/MLS scenes
(< a few hundred trees) the brute-force nearest-neighbour search is fast
enough.  For very large mobile-mapping datasets tile the cloud first.
}
\examples{
\donttest{
LASfile <- system.file("extdata", "TLS_Clip.laz", package = "spanner")
las <- readTLSLAS(LASfile, select = "xyzcr", "-filter_with_voxel 0.01")
sf::st_crs(las) <- 26912

map   <- find_trees(las, use_raster_eigen())
las_v <- tree_points(las, map, method = assign_voronoi())
}

}
\seealso{
\code{\link[=tree_points]{tree_points()}}, \code{\link[=assign_crop]{assign_crop()}}, \code{\link[=assign_graph]{assign_graph()}}
}
