% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{branch_metrics}
\alias{branch_metrics}
\title{Score and flag branch-candidate points from eigen metrics}
\usage{
branch_metrics(em, min_angle = 45, max_angle = 90, score_quantile = 0.75)
}
\arguments{
\item{em}{data.table returned by \code{\link[=eigen_metrics]{eigen_metrics()}}.  Must contain columns
\code{Linearity}, \code{Anisotropy}, \code{Sphericity}, \code{E1x}, \code{E1y}, \code{E1z}.
Optionally \code{Zref} (height above ground, m) for the DBH-height filter.}

\item{min_angle}{numeric.  Minimum axis-angle-from-vertical (°) for a branch
candidate.  Default \code{45}.}

\item{max_angle}{numeric.  Maximum axis-angle-from-vertical (°).  Default
\code{90} (horizontal).}

\item{score_quantile}{numeric [0, 1].  Quantile of \code{BranchScore} used as
the binary threshold for \code{IsBranchCandidate}.  Default \code{0.75}.}
}
\value{
The input data.table with three new columns appended in-place:
\code{AxisAngle}, \code{BranchScore}, \code{IsBranchCandidate}.
}
\description{
\code{branch_metrics} takes the data.table returned by \code{\link[=eigen_metrics]{eigen_metrics()}} (which
must include the \code{E1x}, \code{E1y}, \code{E1z} columns added in spanner >= 1.1.0) and
returns it with three additional columns:
}
\details{
\itemize{
\item \code{AxisAngle} — acute angle (°) between the dominant local axis (eigenvector
of λ1) and the vertical.  0° = perfectly vertical (stem-like); 90° =
horizontal branch.
\item \code{BranchScore} — min–max scaled 0–1 composite score:
\verb{Linearity × Anisotropy × (1 − Sphericity) × sqrt(E1x² + E1y²)}.
The horizontal-component term \verb{sqrt(E1x² + E1y²)} encodes orientation
directly in the score rather than relying on a hard angle filter alone;
it is high when the principal branch axis is oblique or horizontal and
near zero for upright stem-like features.
\item \code{IsBranchCandidate} — logical flag: \code{BranchScore >= score_quantile} \strong{and}
\code{AxisAngle} within \verb{[min_angle, max_angle]} (and optionally \code{Zref > 1.37}
if a \code{Zref} column is present).
}
}
\seealso{
\code{\link[=eigen_metrics]{eigen_metrics()}}
}
