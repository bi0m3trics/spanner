% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Stem_Classification.R
\name{classify_stem_points}
\alias{classify_stem_points}
\title{Classify stem points within a segmented TLS point cloud}
\usage{
classify_stem_points(
  las,
  tree_locs,
  method = c("combined", "eigen", "lewos", "hough"),
  z_min = 0.1,
  z_max = 8,
  search_radius = NULL,
  neigh_k = 30L,
  linearity_threshold = 0.65,
  verticality_threshold = 0.7,
  n_propagation = 3L,
  k_propagation = 10L,
  hough_min_radius = 0.03,
  hough_max_radius = 0.6,
  hough_min_votes = 20L,
  ncpu = 4L,
  voxel_thin = NULL
)
}
\arguments{
\item{las}{LAS object. Must contain a \code{treeID} column (output of
\code{\link[=segment_graph]{segment_graph()}}).}

\item{tree_locs}{sf object of tree seed locations (output of
\code{\link[=get_raster_eigen_treelocs]{get_raster_eigen_treelocs()}}).}

\item{method}{character. One of \code{"combined"} (default), \code{"eigen"},
\code{"lewos"}, or \code{"hough"}.}

\item{z_min}{numeric. Lower Z bound (m above ground) of the stem search
window. Default \code{0.1}.}

\item{z_max}{numeric. Upper Z bound (m). Default \code{8.0}.}

\item{search_radius}{numeric. Maximum XY distance (m) from tree seed XY to
include a point as a stem candidate. Defaults to \code{max(tree_locs$Radius) * 3} when \code{NULL}.}

\item{neigh_k}{integer. Number of nearest neighbours (excluding the query
point itself) for the local PCA neighbourhood used in \code{"eigen"} /
\code{"lewos"}. Default \code{30L}.}

\item{linearity_threshold}{numeric [0, 1]. Minimum Linearity for a point to
be considered wood. Default \code{0.65}.}

\item{verticality_threshold}{numeric [0, 1]. Minimum Verticality for a
point to be considered wood. Default \code{0.70}.}

\item{n_propagation}{integer. Label-propagation iterations (\code{"lewos"}
method). Default \code{3L}.}

\item{k_propagation}{integer. Number of neighbours for propagation (\code{"lewos"}
method). Default \code{10L}.}

\item{hough_min_radius}{numeric. Minimum cylinder radius (m) for Hough
detection. Default \code{0.03}.}

\item{hough_max_radius}{numeric. Maximum cylinder radius (m). Default \code{0.60}.}

\item{hough_min_votes}{integer. Minimum Hough accumulator votes to accept a
detection. Default \code{20L}.}

\item{ncpu}{integer. CPU cores for parallel eigen computation. Default \code{4L}.}

\item{voxel_thin}{numeric or \code{NULL}. If non-NULL, the point cloud is
voxel-thinned to this resolution (m) with \code{\link[lidR:decimate_points]{lidR::decimate_points()}} and
\code{\link[lidR:random_per_voxel]{lidR::random_per_voxel()}} \strong{before} \code{\link[=eigen_metrics]{eigen_metrics()}} is run. The
resulting Linearity/Verticality labels are then projected back to the full
cloud via 1-NN. Dramatically reduces computation time for very dense TLS.
Default \code{NULL} (no thinning).}
}
\value{
A LAS object identical to \code{las} with two extra columns:
\describe{
\item{\code{Stem}}{logical — \code{TRUE} for stem-classified points.}
\item{\code{WoodProb}}{numeric [0, 1] — classification confidence.}
}
}
\description{
\strong{Deprecated in spanner v2.0 — use \code{\link[=stem_eigen]{stem_eigen()}} or \code{\link[=stem_hough]{stem_hough()}} inside
\code{\link[=stem_points]{stem_points()}} instead.}

\code{classify_stem_points} labels every point in a segmented LAS object as
stem (wood stem) or non-stem and attaches two new columns—\code{Stem} (logical)
and \code{WoodProb} (numeric, 0–1)—to the returned LAS. It is designed to
operate on the output of \code{\link[=segment_graph]{segment_graph()}} and feeds directly into
\code{\link[=segment_stem]{segment_stem()}}.
}
\details{
Four classification methods are available:
\describe{
\item{\code{"eigen"}}{Computes local PCA eigenvalue metrics (Linearity and
Verticality) via \code{\link[=eigen_metrics]{eigen_metrics()}} for each point. Points whose
Linearity \strong{≥} \code{linearity_threshold} \strong{AND} Verticality \strong{≥}
\code{verticality_threshold} within \code{search_radius} of their tree's XY
centroid are labelled as stem. Fastest option; works well for clean TLS
data. Mirrors the \code{stm.eigen.knn} approach in TreeLS.}
\item{\code{"lewos"}}{Like \code{"eigen"} but adds \emph{k}-NN majority-vote label
propagation (\code{n_propagation} iterations, \code{k_propagation} neighbours)
inspired by the leaf–wood separation framework of Wang et al. (2020).
Suitable for mixed leaf-wood scenes where isolated wood points may be
missed by thresholding alone.}
\item{\code{"hough"}}{Uses \code{houghPrimitives::detect_tree_boles()} to identify
cylinder-classified points (shape type 4) per tree. Requires the
\strong{houghPrimitives} package. Robust to noise and partial occlusion.}
\item{\code{"combined"}}{Runs \code{"eigen"} as a fast pre-filter then \code{"hough"} on
the eigen-filtered candidates (logical AND). Requires \strong{houghPrimitives}.
Highest precision; recommended default for clean TLS data.}
}

If \code{eigen_metrics} columns (\code{Linearity}, \code{Verticality}) are already present
in \code{las@data} they are reused without recomputation.
}
\examples{
\donttest{
set_lidr_threads(4)
LASfile <- system.file("extdata", "TLS_Clip.laz", package = "spanner")
las <- readTLSLAS(LASfile, select = "xyzcr", "-filter_with_voxel 0.01")
sf::st_crs(las) <- 26912

tree_locs <- get_raster_eigen_treelocs(las, res = 0.025, pt_spacing = 0.0254,
                                       dens_threshold = 0.25,
                                       neigh_sizes = c(0.25, 0.15, 0.66),
                                       eigen_threshold = 0.75,
                                       grid_slice_min = 1, grid_slice_max = 2,
                                       minimum_polygon_area = 0.005,
                                       cylinder_fit_type = "ransac",
                                       max_dia = 1, SDvert = 0.33)

las_seg <- segment_graph(las, tree_locs, k = 50,
                         distance.threshold = 0.5,
                         use.metabolic.scale = FALSE,
                         ptcloud_slice_min = 0.5, ptcloud_slice_max = 2.5,
                         subsample.graph = 0.1, return.dense = TRUE)

# Classify stem points (eigen method — no houghPrimitives needed)
las_stem <- classify_stem_points(las_seg, tree_locs, method = "eigen")
plot(las_stem, color = "Stem")
}

}
\references{
Wang, D., Momo Takoudjou, S., & Casella, E. (2020). LeWoS: A universal
leaf-wood classification method to facilitate the 3D modelling of large
tropical trees using terrestrial LiDAR. \emph{Methods in Ecology and Evolution},
11(3), 376–389. \doi{10.1111/2041-210X.13342}

de Conto, T., Olofsson, K., Gorgens, E. B., Rodriguez, L. C. E., &
Almeida, G. (2017). Performance of terrestrial and airborne LiDAR for
estimating stem volume and aboveground biomass of individual Pinus taeda
trees. \emph{Computers and Electronics in Agriculture}, 140, 249–255.
\doi{10.1016/j.compag.2017.07.019}
}
\seealso{
\code{\link[=segment_graph]{segment_graph()}}, \code{\link[=segment_stem]{segment_stem()}}, \code{\link[=get_raster_eigen_treelocs]{get_raster_eigen_treelocs()}}
}
