% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LAI_LAD.R
\name{compute_lai}
\alias{compute_lai}
\title{Compute leaf area index from a normalized lidar point cloud}
\usage{
compute_lai(las, res_xy = 10, dz = 1, k = 0.5, min_height = 0)
}
\arguments{
\item{las}{A height-normalized \code{LAS} or \code{LAScatalog}. \code{Z} must represent
height above ground (not raw elevation). The \code{ReturnNumber} attribute must
be present (load with \code{select} containing \code{"r"}) so that the gap-fraction
computation uses first returns only. Single-return scanners where every
point has \code{ReturnNumber == 0} are handled automatically.}

\item{res_xy}{numeric. Grid cell size in the XY plane (m). Default \code{10}.}

\item{dz}{numeric. Vertical layer thickness for LAD profile integration
(m). Default \code{1}.}

\item{k}{numeric. Beer-Lambert extinction coefficient. Default \code{0.5}
(spherical leaf angle distribution).}

\item{min_height}{numeric. Minimum height above ground (m) included in
the LAD profile. Points below this threshold are ignored. Default \code{0}.}
}
\value{
A \link[terra:SpatRaster]{terra::SpatRaster} with three layers:
\describe{
\item{\code{LAI}}{Leaf area index (dimensionless). Vertical integral of
LAD over all height bins.}
\item{\code{LAD_mean}}{Mean LAD (m²/m³) across all positive-valued
profile layers.}
\item{\code{LAD_max}}{Maximum LAD (m²/m³) in the vertical profile.}
}
Grid cells with no returns return \code{NA}.
}
\description{
\code{compute_lai} estimates leaf area index (LAI) and leaf area density (LAD)
per 2-D grid cell using the MacArthur-Horn gap-fraction Beer-Lambert
inversion as implemented in \code{\link[lidR:LAD]{lidR::LAD()}}. The point cloud must be
height-normalized (\verb{Z ≥ 0} above ground). Both a single \code{LAS} object and a
\code{LAScatalog} are accepted; catalog processing is parallelised over chunks.
}
\details{
The MacArthur-Horn method models the vertical gap-fraction profile as a
Beer-Lambert process:
\deqn{
  \mathrm{LAD}(z) = -\frac{\ln\!\bigl(\mathrm{gap\;fraction}(z)\bigr)}{k \cdot dz}
}
where \eqn{k} is the extinction coefficient (default 0.5 for a spherical
leaf angle distribution). LAI is the vertical integral:
\deqn{
  \mathrm{LAI} = \int \mathrm{LAD}(z)\,\mathrm{d}z
              \approx \sum_i \mathrm{LAD}(z_i) \cdot dz
}

For \code{LAScatalog} input, set \code{opt_chunk_size()}, \code{opt_chunk_buffer()}, and
a \code{future::plan()} on the catalog before calling to control parallelism.
A buffer of at least \code{res_xy} is recommended to avoid edge artefacts.
}
\examples{
\donttest{
LASfile <- system.file("extdata", "MixedConifer.laz", package = "lidR")
las <- lidR::readLAS(LASfile)
las <- lidR::normalize_height(las, lidR::tin())
lai <- compute_lai(las, res_xy = 20, dz = 1, k = 0.5)
terra::plot(lai[["LAI"]], main = "MacArthur-Horn LAI")
terra::plot(lai[["LAD_mean"]], main = "Mean LAD (m2/m3)")
}

}
\references{
MacArthur, R.H. & Horn, J.W. (1969). Foliage profiles by vertical
measurements. Ecology 50(5): 802–804. https://doi.org/10.2307/1933693

Tenny JT, Sankey TT, Munson SM, Sánchez Meador AJ, Goetz SJ. (2025).
Canopy and surface fuels measurement using terrestrial lidar single-scan
approach in the Mogollon Highlands of Arizona. International Journal
of Wildland Fire 34, WF24221. https://doi.org/10.1071/WF24221
}
\seealso{
\code{\link[=compute_pad_voxels]{compute_pad_voxels()}}, \code{\link[lidR:LAD]{lidR::LAD()}}
}
