% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Bole_Refit.R
\name{refit_trees}
\alias{refit_trees}
\title{Iteratively refit bole models for trees that failed quality assessment}
\usage{
refit_trees(
  las,
  tree_locs,
  seg_table,
  qual_table,
  strategies = c("relax_ransac", "tighten_ransac", "dbscan_bole", "meanshift_bole"),
  refit_marginal = FALSE,
  max_iterations = 3L,
  z_min = 0.1,
  z_max = NULL,
  dz = 0.5,
  overlap = 0.1,
  search_radius = NULL,
  inlier_tol = 0.03,
  n_ransac = 20L,
  conf = 0.99,
  inlier_frac = 0.85,
  n_best = 25L,
  max_radius = 1,
  min_pts = 10L,
  relax_delta = 0.15,
  dbscan_eps_factor = 2,
  dbscan_min_pts = 5L,
  ms_bandwidth = NULL,
  ms_max_iter = 30L,
  ms_tol = 0.001,
  quality_args = list()
)
}
\arguments{
\item{las}{LAS object (output of \code{\link[=segment_graph]{segment_graph()}}; optionally has \code{Stem}
column from \code{\link[=classify_stem_points]{classify_stem_points()}}).}

\item{tree_locs}{sf object from \code{\link[=get_raster_eigen_treelocs]{get_raster_eigen_treelocs()}}.}

\item{seg_table}{\code{data.frame} from \code{\link[=segment_bole]{segment_bole()}}.}

\item{qual_table}{sf from \code{\link[=assess_tree_quality]{assess_tree_quality()}}.}

\item{strategies}{character vector. Ordered list of strategies to attempt.
Default \code{c("relax_ransac", "tighten_ransac", "dbscan_bole", "meanshift_bole")}.}

\item{refit_marginal}{logical. Also attempt to refit \code{"marginal"} trees.
Default \code{FALSE}.}

\item{max_iterations}{integer. Maximum number of strategy passes per tree.
Default \code{3L}.}

\item{z_min}{numeric. Lower bole height (m). Default \code{0.1}.}

\item{z_max}{numeric. Upper bole height (m). \code{NULL} = per-tree max.}

\item{dz}{numeric. Slice height (m). Default \code{0.5}.}

\item{overlap}{numeric. Slice overlap fraction. Default \code{0.1}.}

\item{search_radius}{numeric. Initial XY search radius (m). \code{NULL} =
auto-detect.}

\item{inlier_tol}{numeric. RANSAC inlier tolerance (m). Default \code{0.03}.}

\item{n_ransac}{integer. Initial RANSAC samples. Default \code{20L}.}

\item{conf}{numeric. RANSAC confidence. Default \code{0.99}.}

\item{inlier_frac}{numeric. Initial inlier fraction. Default \code{0.85}.}

\item{n_best}{integer. RANSAC best-of aggregation. Default \code{25L}.}

\item{max_radius}{numeric. Maximum valid radius (m). Default \code{1.0}.}

\item{min_pts}{integer. Minimum slice points. Default \code{10L}.}

\item{relax_delta}{numeric. Inlier-fraction change per relaxation step.
Default \code{0.15}.}

\item{dbscan_eps_factor}{numeric. DBSCAN \code{eps = mean_radius * dbscan_eps_factor}.
Default \code{2.0}.}

\item{dbscan_min_pts}{integer. DBSCAN \code{minPts}. Default \code{5L}.}

\item{ms_bandwidth}{numeric. Mean-shift kernel bandwidth (m). \code{NULL} =
\code{search_radius / 3}.}

\item{ms_max_iter}{integer. Max mean-shift iterations per slice. Default \code{30L}.}

\item{ms_tol}{numeric. Mean-shift convergence tolerance (m). Default \code{0.001}.}

\item{quality_args}{list. Additional arguments forwarded to
\code{\link[=assess_tree_quality]{assess_tree_quality()}} for re-evaluation after refit. Default \code{list()}.}
}
\value{
A named list with three elements:
\describe{
\item{\code{seg_table}}{Updated segment table (all trees, bad trees replaced).}
\item{\code{qual_table}}{Updated quality table (sf).}
\item{\code{refit_log}}{\code{data.frame} with columns \code{TreeID}, \code{original_class},
\code{strategy_used}, \code{final_class}, \code{n_attempts}.}
}
}
\description{
\code{refit_trees} identifies trees labelled \code{"bad"} (or optionally \code{"marginal"})
in \code{qual_table} and attempts to produce improved segment tables by running
a cascade of refit strategies.  Each strategy is tried in order and the
first strategy that lifts a tree out of the \code{"bad"} class is adopted.  The
function returns an updated segment table, an updated quality table, and a
log of what was done per tree.
}
\details{
Strategies (in cascade order):
\describe{
\item{\code{"relax_ransac"}}{Re-runs \code{\link[=segment_bole]{segment_bole()}} with \code{inlier_frac}
reduced by \code{relax_delta} and \code{n_ransac} doubled.  Useful when the
bole is well-separated but RANSAC is too strict.}
\item{\code{"tighten_ransac"}}{Re-runs \code{\link[=segment_bole]{segment_bole()}} with \code{search_radius}
halved and \code{inlier_frac} increased by \code{relax_delta}.  Useful when
co-mingled understorey points pollute the slice.}
\item{\code{"dbscan_bole"}}{For each height slice of a problem tree, runs
\code{dbscan::dbscan()} on the XY coordinates and keeps only the cluster
whose centroid is closest to the tree seed location.  The filtered
points are then fed back into \code{\link[=segment_bole]{segment_bole()}} with the \code{"ransac"}
algorithm.  Requires the \strong{dbscan} package.}
\item{\code{"meanshift_bole"}}{For each height slice, performs simple
iterative mean-shift (bandwidth = \code{ms_bandwidth}) to locate the
densest XY region and restricts fitting to points within that
neighbourhood.  Does \strong{not} require additional packages.}
}
}
\examples{
\donttest{
# (continuing from assess_tree_quality example)
# refit_result <- refit_trees(las_seg, tree_locs, seg_tbl, qual_tbl)
# final_segs   <- refit_result$seg_table
# final_qual   <- refit_result$qual_table
# print(refit_result$refit_log)
}

}
\seealso{
\code{\link[=segment_bole]{segment_bole()}}, \code{\link[=assess_tree_quality]{assess_tree_quality()}}, \code{\link[=classify_stem_points]{classify_stem_points()}}
}
