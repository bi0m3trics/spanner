% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Stem_Segments_Methods.R
\name{seg_bf_cylinder}
\alias{seg_bf_cylinder}
\title{Stem-segment method: brute-force RANSAC cylinder fit per height slice}
\usage{
seg_bf_cylinder(
  dz = 0.5,
  overlap = 0.1,
  n_ransac = 10L,
  conf = 0.95,
  inlier_frac = 0.9,
  max_angle = 30,
  n_best = 20L,
  max_radius = 1,
  min_pts = 10L,
  use_stem_column = TRUE,
  search_radius = NULL
)
}
\arguments{
\item{dz}{numeric.  Slice thickness (m).  Default \code{0.5}.}

\item{overlap}{numeric [0, 1).  Slice overlap fraction.  Default
\code{0.1}.}

\item{n_ransac}{integer.  RANSAC samples per iteration.
Default \code{10L}.}

\item{conf}{numeric.  RANSAC confidence level.  Default \code{0.95}.}

\item{inlier_frac}{numeric.  Expected inlier fraction.  Default \code{0.90}.}

\item{max_angle}{numeric.  Max tolerated deviation from vertical
(degrees).  Default \code{30}.}

\item{n_best}{integer.  Number of best fits to aggregate.
Default \code{20L}.}

\item{max_radius}{numeric.  Max accepted cylinder radius (m).
Default \code{1.0}.}

\item{min_pts}{integer.  Min slice points.  Default \code{10L}.}

\item{use_stem_column}{logical.  Default \code{TRUE}.}

\item{search_radius}{numeric or \code{NULL}.  Default \code{NULL}.}
}
\value{
A closure of class \code{"spanner_sgs_mtd"} / \code{"function"} with
attribute \code{method_name = "bf_cylinder"}.
}
\description{
Fits 3-D cylinders using a brute-force orientation search combined with
RANSAC — the most robust option for strongly leaning or heavily occluded
stems.  Calls \code{cppCylinderFit()} with \code{method = "bf"}.
}
\examples{
\donttest{
segs <- stem_segments(las_s, map,
          method = seg_bf_cylinder(max_angle = 45, n_best = 25L))
}

}
\seealso{
\code{\link[=stem_segments]{stem_segments()}}, \code{\link[=seg_ransac_cylinder]{seg_ransac_cylinder()}}, \code{\link[=seg_irls_cylinder]{seg_irls_cylinder()}}
}
