% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Stem_Segments_Methods.R
\name{seg_irls_cylinder}
\alias{seg_irls_cylinder}
\title{Stem-segment method: IRLS cylinder fit per height slice}
\usage{
seg_irls_cylinder(
  dz = 0.5,
  overlap = 0.1,
  max_radius = 1,
  min_pts = 10L,
  use_stem_column = TRUE,
  search_radius = NULL
)
}
\arguments{
\item{dz}{numeric.  Slice thickness (m).  Default \code{0.5}.}

\item{overlap}{numeric [0, 1).  Slice overlap fraction.  Default
\code{0.1}.}

\item{max_radius}{numeric.  Max accepted cylinder radius (m).
Default \code{1.0}.}

\item{min_pts}{integer.  Min points in a slice.  Default \code{10L}.}

\item{use_stem_column}{logical.  Default \code{TRUE}.}

\item{search_radius}{numeric or \code{NULL}.  Default \code{NULL}.}
}
\value{
A closure of class \code{"spanner_sgs_mtd"} / \code{"function"} with
attribute \code{method_name = "irls_cylinder"}.
}
\description{
Fits 3-D cylinders using Iteratively Reweighted Least Squares (IRLS).
Faster than RANSAC for clean, low-noise stem data; less robust to outliers
and partial occlusion.  Calls \code{cppCylinderFit()} with \code{method = "irls"}.
}
\examples{
\donttest{
segs <- stem_segments(las_s, map, method = seg_irls_cylinder())
}

}
\seealso{
\code{\link[=stem_segments]{stem_segments()}}, \code{\link[=seg_ransac_cylinder]{seg_ransac_cylinder()}}
}
