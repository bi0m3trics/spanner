% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Bole_Segmentation.R
\name{segment_bole}
\alias{segment_bole}
\title{Segment tree boles by fitting circles across height slices}
\usage{
segment_bole(
  las,
  tree_locs,
  algorithm = c("ransac", "hough", "hough_seed_ransac"),
  z_min = 0.1,
  z_max = NULL,
  dz = 0.5,
  overlap = 0.1,
  use_stem_column = TRUE,
  search_radius = NULL,
  inlier_tol = 0.03,
  n_ransac = 20L,
  conf = 0.99,
  inlier_frac = 0.85,
  n_best = 25L,
  max_radius = 1,
  min_pts = 10L,
  hough_min_radius = 0.03,
  hough_max_radius = 0.6,
  hough_min_votes = 15L,
  voxel_thin = NULL,
  ncpu = 1L
)
}
\arguments{
\item{las}{LAS object. Must have a \code{treeID} column (\code{\link[=segment_graph]{segment_graph()}}
output). Optionally has a \code{Bole} column (\code{\link[=classify_lw_points]{classify_lw_points()}} output) or
a legacy \code{Stem} column (\code{\link[=classify_stem_points]{classify_stem_points()}} output).}

\item{tree_locs}{sf object from \code{\link[=get_raster_eigen_treelocs]{get_raster_eigen_treelocs()}}.}

\item{algorithm}{character. Fitting algorithm: \code{"ransac"} (default),
\code{"hough"}, or \code{"hough_seed_ransac"}.}

\item{z_min}{numeric. Lowest height (m above ground) to model. Default \code{0.1}.}

\item{z_max}{numeric. Highest height to model. \code{NULL} = use per-tree
maximum Z. Default \code{NULL}.}

\item{dz}{numeric. Slice thickness (m). Default \code{0.5}.}

\item{overlap}{numeric [0, 1). Fractional overlap between adjacent slices.
Default \code{0.1}.}

\item{use_stem_column}{logical. Use the \code{Bole} column (preferred) or the
legacy \code{Stem} column (if present) to
restrict which points are fitted. Default \code{TRUE}.}

\item{search_radius}{numeric. Maximum XY distance (m) from tree axis for
point inclusion when \code{Stem} column is absent or \code{use_stem_column = FALSE}.
\code{NULL} = \code{max(tree_locs$Radius) * 3}. Default \code{NULL}.}

\item{inlier_tol}{numeric. Distance from the fitted circle surface (m) that
counts as an inlier. Default \code{0.03}.}

\item{n_ransac}{integer. Points sampled per RANSAC iteration. Default \code{20L}.}

\item{conf}{numeric. RANSAC confidence level. Default \code{0.99}.}

\item{inlier_frac}{numeric. Expected inlier fraction. Default \code{0.85}.}

\item{n_best}{integer. Number of best RANSAC solutions to aggregate.
Default \code{25L}.}

\item{max_radius}{numeric. Reject fitted circles with radius > this value
(m). Default \code{1.0}.}

\item{min_pts}{integer. Minimum points in a slice to attempt a fit.
Default \code{10L}.}

\item{hough_min_radius}{numeric. Minimum Hough cylinder radius (m).
Default \code{0.03}.}

\item{hough_max_radius}{numeric. Maximum Hough cylinder radius (m).
Default \code{0.60}.}

\item{hough_min_votes}{integer. Minimum Hough accumulator votes.
Default \code{15L}.}

\item{voxel_thin}{numeric or \code{NULL}. If non-NULL, the LAS is voxel-thinned
to this resolution (m) using \code{\link[lidR:decimate_points]{lidR::decimate_points()}} with
\code{\link[lidR:barycenter_per_voxel]{lidR::barycenter_per_voxel()}} before circle fitting. The barycenter
method preserves sub-voxel geometry for bole cross-sections. Useful for
very dense TLS to reduce C++ computation time. Default \code{NULL}.}
}
\value{
A \code{data.frame} with one row per (TreeID, Segment) and columns:
\describe{
\item{\code{TreeID}}{integer.}
\item{\code{Segment}}{integer. 1 = lowest slice.}
\item{\code{Z_low}, \code{Z_high}, \code{Z_mid}}{numeric. Slice height bounds/centre.}
\item{\code{X_ctr}, \code{Y_ctr}}{numeric. Fitted circle centre.}
\item{\code{Radius}}{numeric. Fitted circle radius (m).}
\item{\code{Diameter}}{numeric. \verb{2 × Radius} (m).}
\item{\code{RANSAC_err}}{numeric. Mean inlier residual (m).}
\item{\code{N_pts}}{integer. Points in slice.}
\item{\code{N_inliers}}{integer. RANSAC inlier count.}
\item{\code{Inlier_frac}}{numeric. \code{N_inliers / N_pts}.}
\item{\code{Fit_method}}{character. Algorithm used.}
\item{\code{Valid}}{logical. \code{FALSE} if too few points or radius out of range.}
}
}
\description{
\code{segment_bole} divides each segmented tree's bole into overlapping height
slices, fits a 2-D RANSAC circle (or Hough-seeded RANSAC circle) to the
bole points in each slice, and returns a tidy \code{data.frame} with one row per
(TreeID × slice). The result feeds directly into \code{\link[=compute_bole_volume]{compute_bole_volume()}}
and \code{\link[=assess_tree_quality]{assess_tree_quality()}}.
}
\details{
Three fitting algorithms are available:
\describe{
\item{\code{"ransac"} (default)}{Calls the compiled C++ RANSAC circle fitter
(\code{C_ransac_bole_slices}) for all trees simultaneously.
No external dependencies. Fast and robust to outlier points.}
\item{\code{"hough"}}{Uses \code{houghPrimitives::detect_tree_boles()} to label
cylinder points per tree, then derives per-slice circle parameters from
the labelled points. Requires the \strong{houghPrimitives} package. Best
for noisy data where pure RANSAC may drift.}
\item{\code{"hough_seed_ransac"}}{Calls \code{"hough"} to obtain an initial centre
and radius estimate per slice, then narrows the RANSAC search to points
within \verb{2 × hough_radius} of the Hough centre before refitting.
Combines the global optimality of the Hough accumulator with the
precision of RANSAC. Requires \strong{houghPrimitives}.}
}

If the \code{las} object has a \code{Bole} column (from \code{\link[=classify_lw_points]{classify_lw_points()}}) or a
legacy \code{Stem} column (from \code{\link[=classify_stem_points]{classify_stem_points()}})
and \code{use_stem_column = TRUE}, only bole/stem-labelled points are used for
fitting; otherwise all points within \code{search_radius} of the tree seed are
used.
}
\examples{
\donttest{
set_lidr_threads(4)
LASfile <- system.file("extdata", "TLS_Clip.laz", package = "spanner")
las <- readTLSLAS(LASfile, select = "xyzcr", "-filter_with_voxel 0.01")
sf::st_crs(las) <- 26912

tree_locs <- get_raster_eigen_treelocs(las, res = 0.025, pt_spacing = 0.0254,
                                       dens_threshold = 0.25,
                                       neigh_sizes = c(0.25, 0.15, 0.66),
                                       eigen_threshold = 0.75,
                                       grid_slice_min = 1, grid_slice_max = 2,
                                       minimum_polygon_area = 0.005,
                                       cylinder_fit_type = "ransac",
                                       max_dia = 1, SDvert = 0.33)

las_seg  <- segment_graph(las, tree_locs, k = 50,
                          distance.threshold = 0.5,
                          use.metabolic.scale = FALSE,
                          ptcloud_slice_min = 0.5, ptcloud_slice_max = 2.5,
                          subsample.graph = 0.1, return.dense = TRUE)

las_bole <- classify_stem_points(las_seg, tree_locs, method = "eigen")

seg_tbl  <- segment_bole(las_bole, tree_locs)
head(seg_tbl)
}

}
\references{
Fischler, M. A., & Bolles, R. C. (1981). Random sample consensus: a
paradigm for model fitting with applications to image analysis and
automated cartography. \emph{Communications of the ACM}, 24(6), 381–395.
\doi{10.1145/358669.358692}
}
\seealso{
\code{\link[=classify_stem_points]{classify_stem_points()}}, \code{\link[=compute_bole_volume]{compute_bole_volume()}},
\code{\link[=assess_tree_quality]{assess_tree_quality()}}
}
