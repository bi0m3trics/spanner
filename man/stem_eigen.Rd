% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Stem_Points_Methods.R
\name{stem_eigen}
\alias{stem_eigen}
\title{Stem-point method: eigenvalue filtering (+ optional LeWoS propagation)}
\usage{
stem_eigen(...)
}
\arguments{
\item{...}{Named arguments forwarded verbatim to \code{\link[=classify_lw_points]{classify_lw_points()}}.
Key parameters: \code{method} (\code{"lewos"} or \code{"eigen"}), \code{neigh_k},
\code{linearity_threshold}, \code{verticality_threshold}, \code{z_min}, \code{z_max},
\code{n_propagation}, \code{k_propagation}, \code{curvature_threshold},
\code{sphericity_threshold}, \code{branch_r}, \code{coarse_r}, \code{ncpu}, \code{voxel_thin}.}
}
\value{
A closure of class \code{"spanner_stm_mtd"} / \code{"function"} with
attribute \code{method_name = "eigen"}.  When called it returns the input LAS
with columns \code{Stem}, \code{StemProb}, \code{Branch}, \code{BranchProb}, \code{Other} added.
}
\description{
Wraps \code{\link[=classify_lw_points]{classify_lw_points()}} as a composable method object.  Uses local
PCA eigenvalue metrics — primarily Linearity and Verticality — computed
over a k-NN neighbourhood to distinguish stem points from branches and
other returns.
}
\details{
Internally this calls \code{classify_lw_points(method = "lewos")} by default,
which adds a majority-vote label-propagation pass after the initial
threshold filter (Wang et al. 2020 LeWoS framework).  Set \code{method = "eigen"} for a pure threshold-only classification.
}
\examples{
\donttest{
# (continuing from tree_points example)
las_s <- stem_points(las_v, map, method = stem_eigen(neigh_k = 20L))
}

}
\seealso{
\code{\link[=stem_points]{stem_points()}}, \code{\link[=stem_hough]{stem_hough()}}, \code{\link[=classify_lw_points]{classify_lw_points()}}
}
