% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Stem_Points_Methods.R
\name{stem_hough}
\alias{stem_hough}
\title{Stem-point method: Hough slice-stack detector}
\usage{
stem_hough(
  h_base_lo = 1,
  h_base_hi = 2.5,
  h_step = 0.5,
  max_d = 0.5,
  pixel_size = 0.025,
  min_density = 0.1,
  min_votes = 3L
)
}
\arguments{
\item{h_base_lo}{numeric.  Lower bound of the stem seeding zone (m above
ground).  Default \code{1.0}.}

\item{h_base_hi}{numeric.  Upper bound of the seeding zone (m).  Must be
greater than \code{h_base_lo}.  Default \code{2.5}.}

\item{h_step}{numeric.  Slice thickness used for tracking (m).
Default \code{0.5}.}

\item{max_d}{numeric.  Maximum stem diameter (m) — sets the Hough
search radius as \code{max_d / 2}.  Default \code{0.5}.}

\item{pixel_size}{numeric.  Hough accumulator resolution (m).  Default
\code{0.025}.}

\item{min_density}{numeric [0, 1].  Fraction of slice max-count required
for a pixel to vote.  Default \code{0.1}.}

\item{min_votes}{integer.  Minimum Hough votes to accept a circle centre.
Default \code{3L}.}
}
\value{
A closure of class \code{"spanner_stm_mtd"} / \code{"function"} with
attribute \code{method_name = "hough"}.  When called it returns the input LAS
with columns \code{Stem} (logical), \code{Segment} (integer), \code{Radius} (numeric),
\code{Votes} (integer) added.
}
\description{
Uses \code{C_hough_stem_plot()} to detect stem points by tracking the best
circular cross-section through stacked horizontal slices, per tree.  A
point is labelled as a stem point if it lies within the tracked circle's
radius (plus \code{pixel_size} tolerance) at its corresponding height band.
}
\details{
This method is complementary to \code{stm_eigen()}:
\itemize{
\item More robust to mixed-return and high-noise conditions where
eigenvalue thresholds become unreliable.
\item Directly comparable to \code{stm.hough()} from \strong{TreeLS}.
\item Returns \code{Stem}, \code{Segment}, \code{Radius}, and \code{Votes} columns rather
than the Stem/Branch/Other columns of \code{stm_eigen()}.
}
}
\examples{
\donttest{
las_h <- stem_points(las_v, map,
           method = stem_hough(h_base_lo = 1, h_base_hi = 2.5,
                              max_d = 0.5, min_votes = 5L))
}

}
\seealso{
\code{\link[=stem_points]{stem_points()}}, \code{\link[=stem_eigen]{stem_eigen()}}, \code{\link[=classify_lw_points]{classify_lw_points()}}
}
