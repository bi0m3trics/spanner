% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TLS_Pipeline.R
\name{stem_points}
\alias{stem_points}
\title{Classify stem points within a segmented point cloud (stage 3)}
\usage{
stem_points(las, map, method = stem_eigen())
}
\arguments{
\item{las}{LAS object with a \code{treeID} column (output of \code{\link[=tree_points]{tree_points()}}).}

\item{map}{\code{sf} POINT object from \code{\link[=find_trees]{find_trees()}}.}

\item{method}{A method closure created by \code{\link[=stem_eigen]{stem_eigen()}} (default) or
\code{\link[=stem_hough]{stem_hough()}}.}
}
\value{
A LAS object identical to \code{las} with classification columns added
(see the individual method documentation for column names).
}
\description{
Labels each point as stem or non-stem using the chosen method.
This stage is optional; \code{\link[=stem_segments]{stem_segments()}} can operate on any LAS that has
a \code{treeID} column, but stem-point pre-filtering greatly improves fit quality.
}
\details{
Two methods are available:
\describe{
\item{\code{\link[=stem_eigen]{stem_eigen()}}}{Wraps \code{\link[=classify_lw_points]{classify_lw_points()}}.  Uses local PCA
eigenvalue metrics (Linearity, Verticality) with optional LeWoS
label propagation.  Returns \code{Stem}, \code{StemProb}, \code{Branch},
\code{BranchProb}, \code{Other} columns.  Best on dense TLS; pass
\code{neigh_k = 30L} and lower \code{verticality_threshold} for MLS.}
\item{\code{\link[=stem_hough]{stem_hough()}}}{Uses \code{C_hough_stem_plot()} to track circular
cross-sections upward per tree and label points inside the tracked
cylinder.  Returns \code{Stem}, \code{Segment}, \code{Radius}, \code{Votes} columns.
More robust to mixed-return and MLS noise; does not separate branches.}
}
}
\examples{
\donttest{
# Eigen + LeWoS label propagation (TLS-optimised; add neigh_k=30 for MLS)
las_e <- stem_points(las_v, map, method = stem_eigen(neigh_k = 20L))

# Hough slice tracker (preferred for MLS)
las_h <- stem_points(las_v, map,
           method = stem_hough(h_base_lo = 1, h_base_hi = 2.5,
                               max_d = 0.5, min_votes = 5L))
}

}
\seealso{
\code{\link[=tree_points]{tree_points()}}, \code{\link[=stem_eigen]{stem_eigen()}}, \code{\link[=stem_hough]{stem_hough()}},
\code{\link[=classify_lw_points]{classify_lw_points()}}
}
