% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TLS_Pipeline.R
\name{stem_segments}
\alias{stem_segments}
\title{Fit per-segment stem geometry (stage 4)}
\usage{
stem_segments(las, map, method = seg_ransac_cylinder())
}
\arguments{
\item{las}{LAS object with \code{treeID} column (and optionally \code{Stem} /
\code{Stem} column from \code{\link[=stem_points]{stem_points()}}).}

\item{map}{\code{sf} POINT object from \code{\link[=find_trees]{find_trees()}}.}

\item{method}{A method closure created by \code{\link[=seg_ransac_cylinder]{seg_ransac_cylinder()}} (default)
or \code{\link[=seg_irls_cylinder]{seg_irls_cylinder()}}.}
}
\value{
A \code{data.frame} with one row per (TreeID, Segment) containing at
minimum: \code{TreeID} (int), \code{Segment} (int), \code{Z_low}, \code{Z_high}, \code{Z_mid}
(num), \code{Radius} (num, m), \code{Diameter} (num, m), \code{RANSAC_err} (num,
RMS fit residual when available), \code{N_pts} (int), \code{Valid} (logical),
\code{Fit_method} (chr).
}
\description{
Divides each tree stem into height slices and fits a geometric primitive
(circle or cylinder) to each slice, returning a tidy \code{data.frame} with
one row per (TreeID × segment).
}
\details{
Two methods are available (see individual documentation for parameters):
\describe{
\item{\code{\link[=seg_ransac_cylinder]{seg_ransac_cylinder()}}}{3-D RANSAC cylinder per slice — captures
stem lean and axis orientation.  Default.}
\item{\code{\link[=seg_irls_cylinder]{seg_irls_cylinder()}}}{3-D IRLS cylinder — fast for clean data;
less robust to outliers.}
}
}
\examples{
\donttest{
segs_r <- stem_segments(las_e, map,
            method = seg_ransac_cylinder(dz = 0.5, max_angle = 20))

segs_i <- stem_segments(las_e, map, method = seg_irls_cylinder())
}

}
\seealso{
\code{\link[=stem_points]{stem_points()}}, \code{\link[=seg_ransac_cylinder]{seg_ransac_cylinder()}}, \code{\link[=seg_irls_cylinder]{seg_irls_cylinder()}}
}
