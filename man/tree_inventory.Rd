% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TLS_Pipeline.R
\name{tree_inventory}
\alias{tree_inventory}
\title{Compute a TLS/MLS tree inventory (stage 5)}
\usage{
tree_inventory(
  map,
  segmented_las,
  seg_table = NULL,
  vol_table = NULL,
  qual_table = NULL,
  return_sf = FALSE,
  dbh_z_lo = 1.2,
  dbh_z_hi = 1.5
)
}
\arguments{
\item{map}{\code{sf} POINT object from \code{\link[=find_trees]{find_trees()}}.}

\item{segmented_las}{LAS object from \code{\link[=tree_points]{tree_points()}} (or \code{\link[=stem_points]{stem_points()}}).
Must have a \code{treeID} column.}

\item{seg_table}{\code{data.frame} or \code{NULL}.  Output of \code{\link[=stem_segments]{stem_segments()}}
or \code{\link[=segment_stem]{segment_stem()}}.  Required columns: \code{TreeID}, \code{Segment}, \code{Z_low},
\code{Z_high}, \code{Radius}, \code{Valid}.  Default \code{NULL}.}

\item{vol_table}{\code{data.frame} or \code{NULL}.  Output of
\code{\link[=compute_stem_volume]{compute_stem_volume()}}.  When supplied, stem volume is merged directly.
Default \code{NULL}.}

\item{qual_table}{\code{sf} or \code{NULL}.  Output of \code{\link[=assess_tree_quality]{assess_tree_quality()}}.
Default \code{NULL}.}

\item{return_sf}{logical.  \code{TRUE} = return an \code{sf} object with convex-
hull crown polygons as geometry.  \code{FALSE} (default) = return an \code{sf}
object with point geometries.}

\item{dbh_z_lo}{numeric.  Lower height (m) for DBH band extraction.
Currently informational — used by downstream functions.  Default \code{1.2}.}

\item{dbh_z_hi}{numeric.  Upper height (m) for DBH band.
Default \code{1.5}.}
}
\value{
An \code{sf} object with the tree-seed columns from \code{map} plus:
\code{height}, \code{crown_area}, \code{crown_base_height}, \code{crown_volume}, \code{diameter}.
Additional columns from \code{seg_table}, \code{vol_table}, and \code{qual_table} when
supplied.
}
\description{
Final stage of the spanner v2.0 pipeline.  Wraps \code{\link[=process_tree_data]{process_tree_data()}} to
compute per-tree height, crown, and diameter metrics from a segmented point
cloud and optional segment-fitting table.
}
\details{
At minimum, \code{map} and \code{segmented_las} (with \code{treeID} column) are required.
Supplying \code{seg_table} (output of \code{\link[=stem_segments]{stem_segments()}} or \code{\link[=segment_stem]{segment_stem()}})
enables accurate DBH and stem volume computation.

The function normalises the \code{seg_table} structure so that cylinder-method
outputs are accepted by \code{\link[=process_tree_data]{process_tree_data()}} without modification.
}
\examples{
\donttest{
# Complete 5-stage pipeline
LASfile <- system.file("extdata", "TLS_Clip.laz", package = "spanner")
las <- readTLSLAS(LASfile, select = "xyzcr", "-filter_with_voxel 0.01")
sf::st_crs(las) <- 26912

map   <- find_trees(las, use_raster_eigen())
las_t <- tree_points(las,   map, assign_voronoi())
las_s <- stem_points(las_t, map, stem_eigen())
segs  <- stem_segments(las_s, map, seg_ransac_cylinder())
inv   <- tree_inventory(map, las_t, seg_table = segs)
print(inv)
}

}
\seealso{
\code{\link[=find_trees]{find_trees()}}, \code{\link[=tree_points]{tree_points()}}, \code{\link[=stem_points]{stem_points()}},
\code{\link[=stem_segments]{stem_segments()}}, \code{\link[=process_tree_data]{process_tree_data()}}, \code{\link[=segment_stem]{segment_stem()}},
\code{\link[=assess_tree_quality]{assess_tree_quality()}}
}
