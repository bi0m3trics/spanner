% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TLS_Pipeline.R
\name{tree_points}
\alias{tree_points}
\title{Assign point-cloud points to individual trees (stage 2)}
\usage{
tree_points(las, map, method = assign_graph())
}
\arguments{
\item{las}{LAS object.  Height-normalized.}

\item{map}{\code{sf} POINT object from \code{\link[=find_trees]{find_trees()}} (or directly from
\code{\link[=get_raster_eigen_treelocs]{get_raster_eigen_treelocs()}}).}

\item{method}{A method closure created by \code{\link[=assign_voronoi]{assign_voronoi()}} (default),
\code{\link[=assign_crop]{assign_crop()}}, or \code{\link[=assign_graph]{assign_graph()}}.}
}
\value{
A LAS object identical to \code{las} with a new integer column
\code{treeID}.  Points not assigned to any tree receive \code{treeID = 0}.
}
\description{
Given tree seed locations from \code{\link[=find_trees]{find_trees()}}, assigns every point a
\code{treeID} integer column using the chosen spatial assignment method.
}
\details{
Three assignment methods are available:
\describe{
\item{\code{\link[=assign_voronoi]{assign_voronoi()}}}{Nearest-tree Voronoi partition — every point
is assigned, regardless of distance.  Fast (O(N × T) brute force for
typical T < 500).  Best when the entire plot should be fully
partitioned.}
\item{\code{\link[=assign_crop]{assign_crop()}}}{Fixed-radius (or square) crop around each tree
centre.  Conservative — only points close to the stem receive a
\code{treeID}; crown/canopy points may stay at 0.  Good pre-filter before
stem fitting.}
\item{\code{\link[=assign_graph]{assign_graph()}}}{Wraps \code{\link[=segment_graph]{segment_graph()}} — builds a k-NN point
graph and propagates IDs via shortest paths.  Most spatially accurate
for crown delineation; also the most computationally expensive.}
}
}
\examples{
\donttest{
map <- find_trees(las, use_raster_eigen())

# Voronoi – every point gets a tree ID
las_v <- tree_points(las, map, method = assign_voronoi())

# Crop – only points within 1.5 m of each stem
las_c <- tree_points(las, map, method = assign_crop(l = 1.5))

# Graph – full crown-level graph segmentation
las_g <- tree_points(las, map,
           method = assign_graph(k = 50, distance.threshold = 0.5,
                                 subsample.graph = 0.1,
                                 return.dense = TRUE))
}

}
\seealso{
\code{\link[=find_trees]{find_trees()}}, \code{\link[=assign_voronoi]{assign_voronoi()}}, \code{\link[=assign_crop]{assign_crop()}},
\code{\link[=assign_graph]{assign_graph()}}, \code{\link[=segment_graph]{segment_graph()}}
}
