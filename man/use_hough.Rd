% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tree_Map_Methods.R
\name{use_hough}
\alias{use_hough}
\title{Tree-map method: Hough slice-stack}
\usage{
use_hough(
  min_h = 1,
  max_h = 3,
  h_step = 0.5,
  pixel_size = 0.025,
  max_d = 0.5,
  min_density = 0.1,
  min_votes = 3L
)
}
\arguments{
\item{min_h}{numeric.  Lower height bound for slice scanning (m).
Default \code{1.0}.}

\item{max_h}{numeric.  Upper height bound (m).  Must be > \code{min_h}.
Default \code{3.0}.}

\item{h_step}{numeric.  Slice thickness (m).  Default \code{0.5}.}

\item{pixel_size}{numeric.  Hough accumulator grid resolution (m).
Smaller values give finer radius precision but increase memory.
Default \code{0.025}.}

\item{max_d}{numeric.  Maximum stem diameter searched (m).  This sets
the maximum circle radius as \code{max_d / 2}.  Default \code{0.5}.}

\item{min_density}{numeric [0, 1].  Minimum fraction of the local
maximum point count for a pixel to contribute a Hough vote.
Default \code{0.1}.}

\item{min_votes}{integer.  Minimum accumulated votes to accept a circle
centre.  Lower values find more (possibly false) stems; higher values
are more conservative.  Default \code{3L}.}
}
\value{
A closure of class \code{"spanner_map_mtd"} / \code{"function"} with
attribute \code{method_name = "hough"}.  When called with a LAS object it
returns an sf POINT object with columns \code{TreeID}, \code{Radius}, \code{Error}
identical in format to \code{\link[=get_raster_eigen_treelocs]{get_raster_eigen_treelocs()}} output.
}
\description{
Detects tree positions by scanning stacked horizontal slices with the
circular Hough Transform.  Finds vertically persistent circular
cross-sections and returns one representative tree centre per detected stem.
}
\details{
The algorithm mirrors \code{map.hough()} from the \strong{TreeLS} package but runs
entirely on spanner's own compiled C++ back end.  Key design choices:
\itemize{
\item Height slices of thickness \code{h_step} are scanned between \code{min_h}
and \code{max_h}.
\item The Hough accumulator casts votes at radius increments of
\code{pixel_size} up to \code{max_d / 2}.
\item A circle centre is accepted if it accumulates at least \code{min_votes}
and the local pixel count exceeds \code{min_density * max_count}.
\item A candidate is promoted to a tree only when it appears in at least
75 \% of the height slices (vertical persistence filter).
}
}
\examples{
\donttest{
LASfile <- system.file("extdata", "TLS_Clip.laz", package = "spanner")
las <- readTLSLAS(LASfile, select = "xyzcr", "-filter_with_voxel 0.01")
sf::st_crs(las) <- 26912

map <- find_trees(las, method = use_hough(
  min_h = 1, max_h = 3, h_step = 0.5, max_d = 0.5,
  min_density = 0.1, min_votes = 3
))
}

}
\seealso{
\code{\link[=find_trees]{find_trees()}}, \code{\link[=use_raster_eigen]{use_raster_eigen()}}
}
